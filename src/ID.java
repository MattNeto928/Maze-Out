
public enum ID { // Collection of identifications for each type of object

	Player(), Block(), Door(), Boost(), BoostDown(), BoostRight(), BoostLeft(), InvBlock(), Key(), Wood(), BasicEnemy(), Checkpoint(), Fake();
}
